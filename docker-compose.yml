version : "3.8"

services:
  # backend:
  #   build:
  #     context: .
  #     dockerfile: backend/Dockerfile
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - auth
  #   networks:
  #     - mynetwork
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    depends_on:
      - auth
    networks:
      - mynetwork
    env_file:
      - ./frontend/.env
  auth:
    image: authimg
    ports:
      - "3000:3000"
    networks:
      - mynetwork
    env_file:
      - ./auth_server/.env

networks:
  mynetwork:
    driver: bridge